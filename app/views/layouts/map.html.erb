<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<title><%= page_title %> | Went Hiking</title>
<meta name="description" content="<%= yield(:meta) || 'Hiking &amp; backpacking trip logs made easy.' %>" />
<%= stylesheet_link_tag :application %>
<script src="http://maps.google.com/maps/api/js?sensor=false&amp;v=3.4&amp;libraries=geometry&amp;language=en" type="text/javascript"></script>
<%= javascript_include_tag :application %>
<style type="text/css">
	.day {
		float:left;
		width:75px;
		margin-right:10px;
		position:relative;
		text-align:center;
	}
	.day .weather_summary {
		color:#999;
		font-size:75%;
		height:35px;
	}
	.day .high {
		color:#671D21;
		font-size:125%;
	}
	.day .low {
		color:#5FA8CC;
	}
	.day .forecast_title {
		font-weight:bold;
		margin-bottom:10px;
	}
</style>
<%= yield :head %>
<%= render :partial => 'layouts/analytics' %>
</head>
<body>
	<div id="navigation">
	<div class="container">
		<%= link_to(image_tag("went_hiking.png", :alt => "Went Hiking"), root_path, :id => "logo") %>
		<div class="nav">
			<a onclick="beef.polylines.startDrawing();return false;" href="#">
				<%= image_tag "pencil_add.png", alt: "start drawing", title: "start drawing" %>
				draw path
			</a>
			<a onclick="beef.polylines.stopDrawing();return false;" href="#">
				<%= image_tag "pencil_delete.png", alt: "stop drawing", title: "stop drawing" %>
				stop drawing
			</a>
			<a onclick="beef.polylines.stepBack();return false;" href="#">
				<%= image_tag "arrow_undo.png", alt: "undo", title: "undo" %>
				undo
			</a>
			<!-- <a onclick="$('form').first().submit();return false;" href="">
					<%= image_tag "disk.png", alt: "save", title: "save" %>
					save
				</a> -->
			
			<span class="nav-unit">
				<input type="search" id="geolocate" placeholder="Search the map…" />
				<%= image_submit_tag "magnifier.png", :id => "search_link" %>
			</span>
		</div>
	</div>
	</div>
	<%- flash.each do |name, msg| -%>
     		<%= content_tag :div, msg, :id => "flash_#{name}" %>
   	<%- end -%>
	<%= yield %>
</body>
</html>