<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<title><%= h(yield(:title) || "Untitled") %> | Went Hiking</title>
<%= stylesheet_link_tag :all %>
<%= javascript_include_tag :defaults %>
<script src="http://maps.google.com/maps/api/js?sensor=false&amp;v=3.4&amp;libraries=geometry&amp;language=en" type="text/javascript"></script>
<%= javascript_include_tag "cufon", "helvetica_300.font", "jquery.easing", "jquery.fancybox", "scripts" %>
<%= yield :head %>
<%= render :partial => 'layouts/analytics' %>
</head>
<body>
	<div id="navigation">
	<div class="container">
		<%= link_to(image_tag("went_hiking.png", :alt => "Went Hiking"), root_path) %>
		<div class="nav">
			<% if @route.id.blank? %>
			<a onclick="beef.polylines.startDrawing();return false;" href="#">start drawing</a> |
			<a onclick="beef.polylines.stepBack();return false;" href="#">undo</a> |
			<input type="text" id="title" placeholder="title the hike" /> and
			<a onclick="$('form')[0].submit();return false;" href="#">save</a>
			<% else %>
				<h2><%=h @route.title %></h2>
			<% end %>
		</div>
	</div>
	</div>
	<%- flash.each do |name, msg| -%>
     		<%= content_tag :div, msg, :id => "flash_#{name}" %>
   	<%- end -%>
	<%= yield %>
</body>
</html>