<%= form_for([@trip, @comment], url: hike_comments_path(@trip)) do |f| %>
	<%= f.text_area :body %>
	<%= f.hidden_field :trip_id %>
	<%= f.hidden_field :user_id %>
	<%= f.submit "Add your comment", :class => "super awesome large button" %>
<% end %>