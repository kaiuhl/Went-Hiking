<% page_title "Search results for &ldquo;#{h(params[:q].titleize)}&rdquo;".html_safe %>

<h1><%= @trips.length %> results for &ldquo;<%= h(params[:q].titleize) %>&rdquo;</h1>
<%= render @trips, :include_name => true %>